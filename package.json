{
  "name": "freeq-swarm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Collaborative compute network on freeq — PR-review swarm MVP",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@10.5.1"
}
