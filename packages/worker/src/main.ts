// Entry point for the swarm-worker daemon.
// Phase 0: hello-world. Phase 1: announce sequence + capability ad.

export async function main(): Promise<void> {
  console.log('swarm-worker: phase 0 scaffold (no-op)');
}
