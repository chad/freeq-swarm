// Entry point for the swarm-coordinator daemon.
// Phase 0: hello-world. Phase 1: announce sequence + BUDGET issuance.

export async function main(): Promise<void> {
  console.log('swarm-coordinator: phase 0 scaffold (no-op)');
}
